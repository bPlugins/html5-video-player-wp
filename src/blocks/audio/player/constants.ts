/** Playback rates offered by the speed selector. */
export const SPEEDS = [0.5, 0.75, 1, 1.25, 1.5, 2];

/** Seconds jumped by the skip-back / skip-forward buttons. */
export const SKIP_SECONDS = 10;

/** Seconds moved per arrow key press on a seek slider. */
export const ARROW_SEEK_SECONDS = 5;

/** Waveform bar geometry for the minimal skin, in pixels. */
export const BAR_WIDTH = 2;
export const BAR_GAP = 2;
export const MIN_BARS = 16;
export const MAX_BARS = 160;
